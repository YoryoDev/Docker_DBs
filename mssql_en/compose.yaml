services:

  mssql_en_init:
    image: busybox:latest
    user: "0"
    profiles:
      - mssql-en
    command:
      - sh
      - -c
      - |
        chown -R 10001:0 \
          /var/opt/mssql/data \
          /var/opt/mssql/backup \
          /var/opt/mssql/jobs \
          /var/opt/mssql/log
    volumes:
      - sqlserver25_en_data:/var/opt/mssql/data
      - sqlserver25_en_backup:/var/opt/mssql/backup
      - sqlserver25_en_jobs:/var/opt/mssql/jobs
      - sqlserver25_en_log:/var/opt/mssql/log

  mssql_en:
    image: mcr.microsoft.com/mssql/server:2025-CU3-ubuntu-22.04
    container_name: sqlserver25_en
    hostname: sqlserver25_en
    restart: no
    depends_on:
      mssql_en_init:
        condition: service_completed_successfully
    profiles:
      - mssql-en
    ports:
      - "${BIND_ADDRESS}:1433:1433"
    volumes:
      - sqlserver25_en_data:/var/opt/mssql/data
      - sqlserver25_en_backup:/var/opt/mssql/backup
      - sqlserver25_en_jobs:/var/opt/mssql/jobs
      - sqlserver25_en_log:/var/opt/mssql/log
      - ./config/mssql.conf:/var/opt/mssql/mssql.conf:ro
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_PID: "StandardDeveloper"
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD}
      MSSQL_AGENT_ENABLED: "true"
      MSSQL_COLLATION: "Latin1_General_100_CI_AS_SC"
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$${MSSQL_SA_PASSWORD}\" -Q 'SELECT 1' -No -l 5"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 2.5G
          cpus: "1.5"
        reservations:
          memory: 512M
          cpus: "0.5"

volumes:
  sqlserver25_en_data:
  sqlserver25_en_backup:
  sqlserver25_en_jobs:
  sqlserver25_en_log:
